#include "main.h"
#include <ctype.h>

/**
 * _isupper -Entry of program
 *
 * @c: int c
 *
 * Return: 0
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
